# Static DU + JaCoCo composite -- Java 8

One minimal file with a dead definition (unusedNote) and a branch,
analyzed by both a static unused-assignment check and JaCoCo branch
coverage in the same run.

Run: bash run_composite.sh
