# API Overview (Java 25)

Core domains:
- items, products, categories, customers, orders, shipments
- events, payments, notifications, warehouses, catalog
- audit, analytics, policies, reports, runtime

Tools are integrated via Maven plugins and scripts (not separate tool folders).
