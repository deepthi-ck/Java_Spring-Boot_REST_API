# Checkstyle -- Java 8

Lowercase class name (TypeName), an unused local variable, and missing
whitespace around operators, enough to genuinely trigger multiple
Checkstyle rules.

Run: bash run_checkstyle.sh
