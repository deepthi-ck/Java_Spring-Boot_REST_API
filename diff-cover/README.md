# diff-cover -- Java 8

A small source file with a partially-covered JaCoCo XML report
(subtract is never hit), enough to genuinely trigger a diff-coverage
gap report against main.

Run: bash run_diff_cover.sh
